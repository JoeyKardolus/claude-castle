You are helping a beginner (a father and son) finish setting up their Nextcloud at `cloud.{their-domain}`. The stack is already running on their server (see `SERVER_IP` and domain in `config/castle.env`; secrets in `/opt/castle/castle.env` on the server). Explain each phase in one plain sentence before running it. Run remote steps over ssh as the `castle` user, using the Nextcloud container's `occ` command.

1. Verify Nextcloud responds at `https://cloud.{domain}` and the container is healthy. If not, stop and suggest `prompts/07-troubleshoot.md`.
2. Admin account: check whether the admin user already exists. If not, create it using the Nextcloud admin password stored in `/opt/castle/castle.env`. Never print the password in chat; tell the user where it lives and how to read it. Explain: admin is for settings only, not for daily files.
3. Enforce two-factor login by running the script at `infra/nextcloud/` in the repo (it enables the TOTP app and turns on enforcement for all users). Explain in one sentence: from now on every login needs a password plus a 6-digit code from a phone app, so a stolen password alone is useless.
4. Create one everyday account for each of the two of them. Ask for the two usernames and an email per account. Generate a strong temporary password for each, store both in a clearly named file the user chooses (or read them out once and tell them to save them in a password manager), and enable "must change password" if available.
5. Have the first user log in at `https://cloud.{domain}` now, live, while you wait. Talk them through the 2FA enrolment: install Google/Microsoft Authenticator (or any TOTP app) on their phone, scan the QR code, enter the 6-digit code. Confirm they reached the files screen. Then the second user, same steps, own phone.
6. Remind them to save their 2FA backup codes somewhere safe, and explain in one sentence why (phone lost = backup code is the way back in).
7. Point them to the Nextcloud mobile app (App Store / Play Store), server address `https://cloud.{domain}`, and mention automatic photo upload as an option.

At the end, report what you did in plain words: which accounts exist, that 2FA is enforced and both of them enrolled, where the passwords are stored, and what to do if a phone is lost.
