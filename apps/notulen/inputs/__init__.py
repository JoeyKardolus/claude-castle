"""Top-level static input assets shared between notulen runtimes.

``notulen_template.md`` is the meeting-minutes template used by both the
dashboard FastAPI app (CPU fallback) and the GPU worker.
"""
